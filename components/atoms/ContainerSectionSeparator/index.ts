import styled from 'styled-components'

import { Separator } from '../BoxView'

const ContainerSectionSeparator = styled(Separator)`
  margin-top: 0.5rem;
  border-radius: 50%;
`

export default ContainerSectionSeparator
